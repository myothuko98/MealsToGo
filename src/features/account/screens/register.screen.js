import React from "react";
import { AccountCover, AccountImageBackground } from "../components/account.styles";

export const RegisterScreen = () => {
  return (
    <AccountImageBackground>
        <AccountCover></AccountCover>
    </AccountImageBackground>
  );
};
